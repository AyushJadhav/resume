$(document).ready(function() {
    $('.navbar').addClass('fade-in');
    $('.container').addClass('fade-in');
  });

  function openTab(th)
            {
                window.open(th.name,'_blank');
            }